# Database
# For PostgreSQL (used on Render):
DATABASE_URL=postgres://user:password@host:5432/database
# For local development with SQLite, leave DATABASE_URL unset

# Auth
AUTH_BASE_URL=http://localhost:3000/api/auth

# CORS trusted origins (comma-separated)
TRUSTED_ORIGINS=http://localhost:5173,http://localhost:5174,http://localhost:3000

# Server
PORT=3000
HOST=0.0.0.0
NODE_ENV=development
